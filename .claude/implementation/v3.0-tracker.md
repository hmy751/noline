# v3.0 Implementation Tracker

**설계**: ✅ 2025-11-20 완료
**구현**: 🚧 진행중 (Phase 1, 2, 3, 4 핵심 완료 - 90%)
**완료**: 미정

---

## 📋 설계 문서

- [x] [policy-architecture.md](../core/policy-architecture.md) (520줄)
- [x] [manual-input.md](../features/manual-input.md) (568줄)
- [x] [data-service-separation.md](../decisions/2025-11-20-data-service-separation.md)

---

## 🚧 구현 체크리스트

### Phase 1: Policy Layer Core ✅

**목표**: Policy Layer 기본 구조 구현 (CRUD-Centric)

**완료일**: 2025-11-20

#### 초기 구현

- [x] `apps/client/src/shared/policy/types.ts` 생성
  - PolicyKey 타입 정의 (4 states)
  - CRUDPermission, CRUDOperationPolicies 인터페이스 정의
  - AppPolicyContext 타입 정의
- [x] `apps/client/src/shared/policy/constants.ts` 생성
  - CRUD-Centric 구조: TRIP_POLICIES, SCHEDULE_POLICIES, EXPENSE_POLICIES
  - 각 동작(create/read/update/delete)별로 4가지 상태 정의
  - SERVICE_POLICIES 별도 관리
- [x] `apps/client/src/shared/policy/useAppPolicy.ts` 생성
  - 단일 Hook으로 모든 정책 제공
  - PolicyKey 계산 로직 내장
  - useNetworkStatus() 통합
  - getTripActivationStatus() 통합
- [x] `apps/client/src/shared/policy/errors.ts` 생성
  - PolicyError 클래스
  - createPolicyError() 헬퍼
- [x] `apps/client/src/shared/policy/index.ts` 생성
  - Clean exports (useAppPolicy만 export)

#### CRUD-Centric 리팩토링

- [x] **구조 변경**: State-Centric → CRUD-Centric
  - 변경 이유: 각 동작이 4가지 상태에서 어떻게 동작하는지 한눈에 파악
  - 변경 전: `POLICY_TABLE[state].createSchedule`
  - 변경 후: `SCHEDULE_POLICIES.create[state]`
- [x] **usePolicyKey 제거**
  - PolicyKey 계산 로직을 useAppPolicy에 직접 통합
  - 불필요한 중간 레이어 제거
- [x] **useSchedulePolicy 제거**
  - Entity별 Hook 불필요 (useAppPolicy로 통합)
  - 사용법: `policy.schedule.create.allowed`

#### 실제 적용

- [x] `CreateScheduleScreen`에 Policy 적용 (3가지 패턴)
  - 패턴 1: 생성 불가 시 화면 차단
  - 패턴 2: Manual-only mode 안내 메시지
  - 패턴 3: Full mode에서만 검색창 표시
- [x] `useCreateSchedule` Hook 순수성 유지 (Policy 체크 제거)

#### 문서화

- [x] policy-architecture.md 업데이트 (CRUD-Centric 반영)
- [x] policy-layer-separation.md 업데이트 (리팩토링 이력 추가)
- [x] v3.0-tracker.md 업데이트

#### 남은 작업

- [ ] 단위 테스트 작성 (TODO)
  - SCHEDULE_POLICIES.create 검증
  - useAppPolicy 훅 테스트

#### Phase 1 강화 (2025-01-18)

- [x] **Query Hook 캐시 폴백 제거** (정책 일관성 강화)
  - 변경 이유: 비활성+오프라인 시 캐시 폴백이 정책과 충돌
  - 적용: `useGetTrips`, `useGetSchedules`, `useGetTripExpenses`
  - 정책대로: 비활성+오프라인 → OfflineError → "활성화하기" 안내
- [x] **useAppPolicy Hook 개선**
  - `useGetTripActivation` 재사용으로 중복 제거
  - 직접 DB 조회 → 기존 Hook 재사용 (Single Source of Truth)
- [x] **문서화**
  - `selective-activation-architecture.md`에 "패턴 7" 추가
  - 비활성+오프라인 시나리오와 React Query 캐시 관계 설명

**완료 조건**: `useAppPolicy()`가 4가지 상태에 따라 올바른 정책 반환 ✅

**최종 구조**:

```typescript
const policy = useAppPolicy(tripId);
policy.schedule.create.allowed; // ✅ CRUD-Centric 접근
policy.service.mapProvider; // ✅ Service 정책
```

---

### Phase 2: Service Layer 적용 ✅

**목표**: Map, Search, Directions를 Policy Layer로 제어

**완료일**: 2025-11-20

- [x] **SmartMapView 컴포넌트** 리팩토링
  - `policy.service.mapProvider` 사용
  - Google Maps / Mapbox 자동 전환
  - `none` 케이스 추가 (offline_inactive 상태)
  - 3가지 상태 처리: 'google', 'mapbox', 'none'
- [x] **CreateScheduleScreen** 검증
  - Phase 1에서 이미 policy 통합 완료
  - `policy.schedule.create.mode` 사용
  - manual-only 모드에서 검색창/검색 결과 숨김
  - 안내 메시지 표시
- [x] **DirectionsAPI** 결정
  - 현재 패턴 유지 (Component-Level Policy 체크)
  - Service는 순수 함수로 유지
  - Phase 1 패턴 준수: "Policy in Components, not Services"

**완료 조건**: 활성화 상태에 따라 지도 제공자 자동 전환 ✅

**구현 상세**:

```typescript
// SmartMapView.tsx
const policy = useAppPolicy(tripId ?? undefined);

switch (policy.service.mapProvider) {
  case 'mapbox':
    return <OfflineMapView ... />;
  case 'none':
    return <NoMapAvailable message="..." />;
  case 'google':
  default:
    return <RNMapView provider={PROVIDER_GOOGLE} ... />;
}
```

---

### Phase 3: Manual Input 구현

**목표**: 오프라인에서 핵심 데이터 입력 가능

#### 3.1 Schedule Manual Input ✅

**완료일**: 2025-11-20

- [x] **ManualScheduleForm** 컴포넌트 생성
  - 파일: `apps/client/src/features/schedule/create-schedule/ManualScheduleForm.tsx`
  - latitude/longitude nullable 처리
  - 장소 이름/주소 텍스트 입력
  - 오프라인 안내 배너 표시
- [x] **CreateScheduleScreen** 통합
  - manual-only 모드에서 ManualScheduleForm 표시
  - DatePicker/TimePicker 조건 수정
- [x] **useCreateScheduleForm** 수정
  - selectedLocation 없을 때 latitude/longitude null 자동 처리

**구현 상세**:

```typescript
// ManualScheduleForm - 오프라인 전용 폼
<ManualScheduleForm
  form={form}
  onShowTimePicker={handleShowTimePicker}
  onSubmit={onSubmit}
  onCancel={handleBackPress}
  isPending={isPending}
/>
```

#### 3.2 Expense Manual Input ✅

**완료일**: 2025-11-20

- [x] **ManualExpenseForm** 컴포넌트 생성
  - 파일: `apps/client/src/features/expense/create-expense/ManualExpenseForm.tsx`
  - 환율 정보 없이 입력 가능
  - 통화 선택 UI
  - 오프라인 안내 배너 표시
- [x] **CreateExpenseScreen** 통합
  - manual-only 모드에서 ManualExpenseForm 표시
  - Policy 체크 추가
- [x] **index.ts** export 추가
  - ManualExpenseForm export

**구현 상세**:

```typescript
// CreateExpenseScreen - Policy 기반 조건부 렌더링
{policy.expense.create.mode === 'manual-only' ? (
  <ManualExpenseForm ... />
) : (
  <ExpenseForm ... />
)}
```

#### 3.3 장소 재검색 기능 ✅

**완료일**: 2025-11-20

**접근 방식**: 자동 보강 대신 수동 재검색 (더 정확함)

- [x] **LocationSearchModal** 컴포넌트 생성
  - 파일: `apps/client/src/features/schedule/update-schedule/LocationSearchModal.tsx`
  - 기존 LocationSearchBar/Results 재사용
  - Modal 형태로 전체 화면 검색
- [x] **UpdateScheduleDrawer** 확장
  - 장소 재검색 기능 추가
  - Policy 연동: `policy.service.searchMode === 'api'` (온라인에서만)
  - latitude 없을 때 경고 + 검색 버튼 표시
  - useUpdateSchedule로 location/latitude/longitude 업데이트
- [x] **ScheduleCard** 경고 표시
  - `showLocationWarning` prop 추가
  - 좌표 없고 온라인일 때 경고 메시지
  - "일정 수정에서 장소를 검색하세요"

**구현 상세**:

```typescript
// UpdateScheduleDrawer - 장소 재검색
{!scheduleData.latitude && scheduleData.location && (
  <View className='bg-yellow-50 ...'>
    <Text>⚠️ 좌표가 없습니다</Text>
    {policy.service.searchMode === 'api' && (
      <TouchableOpacity onPress={() => setLocationSearchVisible(true)}>
        <Text>검색</Text>
      </TouchableOpacity>
    )}
  </View>
)}

// ScheduleCard - 경고 표시
<ScheduleCard
  latitude={schedule.latitude}
  showLocationWarning={isOnline}
  ...
/>
```

**완료 조건**: 온라인에서 Manual Input 일정의 장소 재검색 가능 ✅

---

### Phase 4: 기존 코드 통합

**목표**: 기존 컴포넌트/폼을 Policy Layer와 통합

#### 4.1 ExpenseForm 통합 ✅

**완료일**: 2025-11-21

- [x] **ExpenseForm.tsx** 업데이트
  - 파일: `apps/client/src/features/expense/create-expense/ExpenseForm.tsx`
  - `useAppPolicy(tripId)` Hook 추가
  - 일정 연결 섹션에 Policy 체크 추가
  - `policy.schedule.read.allowed` 체크 → 불가 시 안내 메시지
  - `schedulesFetchError` 에러 처리 추가
  - AlertCircle 아이콘으로 오프라인 안내
- [x] **일정 연결 기능** Policy 적용
  - offline_inactive: "오프라인 상태에서는 일정 연결 기능을 사용할 수 없습니다"
  - API 에러: "일정 목록을 불러오는데 실패했습니다"
  - 정상 작동: 기존 Select UI 유지

**구현 상세**:

```typescript
// ExpenseForm - Policy 기반 일정 연결
{!policy.schedule.read.allowed ? (
  <View className='bg-yellow-50 border border-yellow-200 ...'>
    <AlertCircle size={16} color='#D97706' />
    <Text>오프라인 상태에서는 일정 연결 기능을 사용할 수 없습니다</Text>
  </View>
) : schedulesFetchError ? (
  <View className='bg-red-50 border border-red-200 ...'>
    <AlertCircle size={16} color='#DC2626' />
    <Text>일정 목록을 불러오는데 실패했습니다</Text>
  </View>
) : (
  <Select ... /> // 정상 작동
)}
```

#### 4.2 TripDateForm 통합 ✅

**완료일**: 2025-11-21

- [x] **TripDateForm.tsx** 네트워크 체크 간소화
  - 파일: `apps/client/src/features/trip/create-trip/TripDateForm.tsx`
  - Full Policy 대신 `useNetworkStatus()` 직접 사용
  - 화면 차단 대신 버튼 비활성화 방식 채택
  - 오프라인 안내 메시지 추가 ("인터넷 연결이 필요합니다")
- [x] **UX 개선**
  - 여행 생성 버튼만 disabled 처리
  - 화면 전체 차단 제거 (불필요한 제약 제거)
  - 간결한 오프라인 안내
- [x] **useAppPolicy Hook 개선**
  - 파일: `apps/client/src/shared/policy/useAppPolicy.ts`
  - Async/await 로직 수정 (fetchActivationStatus 함수)
  - Cleanup function 추가 (isCancelled 플래그)
  - useMemo로 policy 객체 메모이제이션

**구현 상세**:

```typescript
// TripDateForm - 네트워크 상태 기반 버튼 제어
const networkStatus = useNetworkStatus();
const isOnline = networkStatus === 'online';

<Pressable
  variant='default'
  onPress={handleSubmit(onValid, onInvalid)}
  disabled={isPending || !isOnline}
>
  {isPending ? '생성 중...' : '여행 생성'}
</Pressable>

{!isOnline && (
  <View className='mt-xs px-xs'>
    <Text className='text-small text-center text-muted-foreground'>
      인터넷 연결이 필요합니다
    </Text>
  </View>
)}
```

#### 4.3 기존 폼 업데이트 ✅

**완료일**: 2025-11-21

##### 4.3.1 ScheduleForm 검증 ✅

- [x] CreateScheduleScreen에 Policy 이미 적용 확인
- [x] ScheduleForm은 presentational component로 Policy 불필요 확인

##### 4.3.2 NetworkStatusIndicator 통합 ✅

- [x] **NetworkStatusIndicator.tsx** 제대로 구현
  - 파일: `apps/client/src/shared/components/Navigation/NetworkStatusIndicator.tsx`
  - `useNetworkStatus()` Hook 사용
  - 3가지 상태 표시: online (초록), offline (빨강), unknown (회색)
  - MobileHeader 우측에 통합됨
- [x] **OfflineIndicator 제거**
  - 중복 기능으로 불필요
  - NetworkStatusIndicator로 통합 완료

##### 4.3.3 PolicyErrorDisplay 컴포넌트 ✅

- [x] **PolicyErrorDisplay.tsx** 생성
  - 파일: `apps/client/src/shared/components/ErrorBoundary/PolicyErrorDisplay.tsx`
  - 3가지 variant: banner, block, inline
  - CRUDPermission 기반 에러 표시
  - 아이콘 자동 선택 (WifiOff, Lock, AlertCircle)
- [x] **CreateScheduleScreen 적용**
  - block 스타일: 생성 불가 시 화면 차단
  - banner 스타일: manual-only 모드 안내
- [x] **ExpenseForm 적용**
  - inline 스타일: 일정 연결 불가 안내
  - 에러 메시지 표준화

**구현 상세**:

```typescript
// PolicyErrorDisplay - 3가지 variant
<PolicyErrorDisplay permission={policy.schedule.create} variant='block' />   // 화면 차단
<PolicyErrorDisplay permission={policy.schedule.create} variant='banner' />  // 상단 배너
<PolicyErrorDisplay permission={policy.schedule.read} variant='inline' />    // 인라인

// CreateScheduleScreen - block
if (!policy.schedule.create.allowed) {
  return <PolicyErrorDisplay permission={policy.schedule.create} variant='block' />;
}

// CreateScheduleScreen - banner
{policy.schedule.create.mode === 'manual-only' && (
  <PolicyErrorDisplay permission={policy.schedule.create} variant='banner' />
)}

// ExpenseForm - inline
{!policy.schedule.read.allowed && (
  <PolicyErrorDisplay permission={policy.schedule.read} variant='inline' />
)}
```

**완료 조건**: 핵심 기능의 Policy Layer 통합 + 에러 처리 표준화 완료 ✅

---

### Phase 5: 테스트 & 문서화

**목표**: 품질 보장 및 문서 업데이트

- [ ] **통합 테스트**
  - 4가지 상태별 시나리오 테스트
  - Manual Input → 네트워크 복구 → 데이터 보강 흐름
  - Policy 전환 시 UI 업데이트
- [ ] **문서 업데이트**
  - CLAUDE.md: v3.0을 "핵심 아키텍처 (v3.0 - 현재)"로 이동
  - policy-architecture.md: 상태 헤더를 "✅ 구현 완료"로 변경
  - manual-input.md: 상태 헤더를 "✅ 구현 완료"로 변경
  - CHANGELOG.md: "v3.0 구현 완료" 항목 추가
- [ ] **마이그레이션 가이드 검증**
  - policy-architecture.md의 체크리스트 완료 확인
  - 코드 예시가 실제 구현과 일치하는지 확인

**완료 조건**: 모든 테스트 통과, 문서가 실제 구현 반영

---

## 📊 진행 상황

| Phase     | 상태 | 완료율 | 완료일     |
| --------- | ---- | ------ | ---------- |
| Phase 1   | ✅   | 100%   | 2025-11-20 |
| Phase 2   | ✅   | 100%   | 2025-11-20 |
| Phase 3.1 | ✅   | 100%   | 2025-11-20 |
| Phase 3.2 | ✅   | 100%   | 2025-11-20 |
| Phase 3.3 | ✅   | 100%   | 2025-11-20 |
| Phase 4.1 | ✅   | 100%   | 2025-11-21 |
| Phase 4.2 | ✅   | 100%   | 2025-11-21 |
| Phase 4.3 | ✅   | 100%   | 2025-11-21 |
| Phase 5   | ⏳   | 0%     | -          |

**전체 진행률**: 90% (Phase 1~4 완료, Phase 5 남음)\*\*

---

## 🚫 현재 Blocker

- 없음 - 설계 완료, 구현 시작 가능

---

## 🐛 추가 버그 수정 (2025-01)

### 다중 사용자 환경 userId 필터링 ✅

**완료일**: 2025-01-17

**문제**: tripActivations 쿼리에 userId 필터링 누락으로 다른 사용자의 활성화 데이터 조회

**수정 내용**:

- [x] `metadata.ts`: `hasAnyActivatedTrip()`, `getActivatedTripInfo()`에 userId 필터 적용
- [x] `useGetTripActivation.ts`: `useGetActiveTrip()`에 userId 필터 적용
- [x] `selective-activation-architecture.md`: 다중 사용자 환경 userId 필터링 문서화

**구현 상세**:

```typescript
// Before: userId 필터 없음
const result = await db.select().from(tripActivations)
  .where(eq(tripActivations.isActivated, true));

// After: userId 필터 적용
const result = await db.select().from(tripActivations)
  .where(and(
    eq(tripActivations.isActivated, true),
    eq(tripActivations.userId, userId)
  ));
```

---

## 📝 구현 시 참고사항

### 핵심 파일 구조 (CRUD-Centric)

```
apps/client/src/
  shared/
    policy/
      types.ts           # CRUDPermission, AppPolicyContext, CRUD-Centric 타입
      constants.ts       # TRIP/SCHEDULE/EXPENSE_POLICIES (CRUD-Centric)
      useAppPolicy.ts    # 단일 Hook (PolicyKey 계산 포함)
      errors.ts          # PolicyError 클래스
      index.ts           # Clean exports

  entities/
    schedule/
      ui/
        ManualScheduleForm.tsx  # Phase 3
    expense/
      ui/
        ManualExpenseForm.tsx   # Phase 3

  screens/
    CreateScheduleScreen.tsx   # ✅ Policy 적용 완료
```

### 구현 순서 권장사항

1. **먼저 Phase 1 완료** → useAppPolicy() 작동 확인
2. **Phase 2 중 SmartMapView 우선** → 가장 영향 큰 기능
3. **Phase 3 Schedule Manual Input 우선** → Expense보다 복잡
4. **Phase 4는 점진적으로** → 한 번에 하지 말고 하나씩
5. **Phase 5는 마지막** → 모든 구현 완료 후

### 주의사항

- `useAppPolicy()`는 항상 현재 상태 기반으로 실시간 정책 반환
- Policy 변경 시 UI가 자동으로 업데이트되도록 설계
- Manual Input은 "기본 모드"가 아닌 "Fallback"으로 구현
- 네트워크 복구 시 데이터 보강은 백그라운드에서 조용히 수행

### CRUD-Centric 구조의 장점

- **직관성**: 각 동작이 4가지 상태에서 어떻게 동작하는지 한눈에 파악
- **유지보수성**: 정책 변경 시 해당 operation의 4가지 상태만 수정
- **확장성**: 새 Entity 추가 시 CRUDOperationPolicies 인터페이스 활용
- **단순성**: 단일 Hook으로 모든 정책 제공, 불필요한 중간 레이어 없음

### 테스트 시나리오 예시

```typescript
// Scenario 1: 온라인 + 활성화
describe('online_active', () => {
  it('should allow all features', () => {
    const policy = useAppPolicy();
    expect(policy.createTrip.allowed).toBe(true);
    expect(policy.mapProvider).toBe('google');
    expect(policy.searchMode).toBe('api');
  });
});

// Scenario 2: 오프라인 + 활성화
describe('offline_active', () => {
  it('should block trip creation but allow schedule manual input', () => {
    const policy = useAppPolicy();
    expect(policy.createTrip.allowed).toBe(false);
    expect(policy.createSchedule.allowed).toBe(true);
    expect(policy.searchMode).toBe('manual');
  });
});
```

---

## 🔗 관련 문서

- [Policy Architecture Guide](../core/policy-architecture.md) - 구현 가이드 (520줄)
- [Manual Input Guide](../features/manual-input.md) - 기능 명세 (568줄)
- [Decision: Data/Service Separation](../decisions/2025-11-20-data-service-separation.md)
- [CHANGELOG.md](../CHANGELOG.md) - v3.0 항목

---

## 💡 Tips

- 구현 중 막히면 policy-architecture.md의 코드 예시 참고
- Phase 완료 시마다 이 문서의 체크박스 업데이트
- 각 Phase 완료 후 CHANGELOG.md에도 기록 추가
- 구현 완료 후 설계 문서의 상태 헤더를 "✅ 구현 완료"로 변경

# v3.0 Implementation Tracker

**설계**: ✅ 2025-11-20 완료
**구현**: ⏳ 대기중
**완료**: 미정

---

## 📋 설계 문서

- [x] [policy-architecture.md](../.claude/core/policy-architecture.md) (520줄)
- [x] [manual-input.md](../.claude/features/manual-input.md) (568줄)
- [x] [data-service-separation.md](../.claude/decisions/2025-11-20-data-service-separation.md)

---

## 🚧 구현 체크리스트

### Phase 1: Policy Layer Core

**목표**: Policy Layer 기본 구조 구현

- [ ] `apps/client/src/shared/policy/types.ts` 생성
  - PolicyKey 타입 정의
  - PolicyConfig 인터페이스 정의
  - PolicyResult 타입 정의
- [ ] `apps/client/src/shared/policy/constants.ts` 생성
  - POLICY_TABLE 정의 (4가지 상태 매트릭스)
  - 각 상태별 정책 설정
- [ ] `apps/client/src/shared/policy/hooks.ts` 생성
  - useAppPolicy() 훅 구현
  - useNetworkStatus() 통합
  - useTripActivationStatus() 통합
- [ ] 단위 테스트 작성
  - POLICY_TABLE 검증
  - useAppPolicy 훅 테스트

**완료 조건**: `useAppPolicy()`가 4가지 상태에 따라 올바른 정책 반환

---

### Phase 2: Service Layer 적용

**목표**: Map, Search, Directions를 Policy Layer로 제어

- [ ] **SmartMapView 컴포넌트** 리팩토링
  - `policy.mapProvider` 사용
  - Google Maps / Mapbox 자동 전환
  - 오프라인 지도 활성화 로직
- [ ] **PlaceSearch 컴포넌트** 리팩토링
  - `policy.searchMode` 사용
  - 검색 가능 여부 체크
  - Manual Input 모드 전환
- [ ] **DirectionsAPI** 통합
  - `policy.routingMode` 사용
  - Offline routing / Online API 분기

**완료 조건**: 활성화 상태에 관계없이 온라인에서 Google Maps/Search 사용 가능

---

### Phase 3: Manual Input 구현

**목표**: 오프라인에서 핵심 데이터 입력 가능

- [ ] **Schedule Manual Input**
  - `ManualScheduleForm` 컴포넌트 생성
  - latitude/longitude nullable 처리
  - placeName 텍스트 입력
  - 오프라인 표시 UI
- [ ] **Expense Manual Input**
  - `ManualExpenseForm` 컴포넌트 생성
  - 환율 정보 없이 입력 가능
  - 통화 선택 UI
- [ ] **데이터 보강 로직**
  - 네트워크 복구 시 좌표 자동 조회
  - Geocoding API 통합
  - 사용자 확인 UI (선택적)

**완료 조건**: 오프라인에서 Schedule/Expense 생성 가능, 온라인 복구 시 자동 보강

---

### Phase 4: 기존 코드 통합

**목표**: 기존 컴포넌트/폼을 Policy Layer와 통합

- [ ] **기존 폼 업데이트**
  - TripForm에 policy.createTrip 체크 추가
  - ScheduleForm에 policy.searchMode 체크 추가
  - ExpenseForm에 policy.createExpense 체크 추가
- [ ] **UI 표시**
  - 오프라인 모드 인디케이터
  - Manual Input 안내 메시지
  - Policy 제한 사유 표시 (DisabledMessage)
- [ ] **에러 처리**
  - Policy 위반 시 에러 메시지
  - Graceful degradation

**완료 조건**: 모든 기존 기능이 Policy Layer 통합 완료

---

### Phase 5: 테스트 & 문서화

**목표**: 품질 보장 및 문서 업데이트

- [ ] **통합 테스트**
  - 4가지 상태별 시나리오 테스트
  - Manual Input → 네트워크 복구 → 데이터 보강 흐름
  - Policy 전환 시 UI 업데이트
- [ ] **문서 업데이트**
  - CLAUDE.md: v3.0을 "핵심 아키텍처 (v3.0 - 현재)"로 이동
  - policy-architecture.md: 상태 헤더를 "✅ 구현 완료"로 변경
  - manual-input.md: 상태 헤더를 "✅ 구현 완료"로 변경
  - CHANGELOG.md: "v3.0 구현 완료" 항목 추가
- [ ] **마이그레이션 가이드 검증**
  - policy-architecture.md의 체크리스트 완료 확인
  - 코드 예시가 실제 구현과 일치하는지 확인

**완료 조건**: 모든 테스트 통과, 문서가 실제 구현 반영

---

## 📊 진행 상황

| Phase   | 상태 | 완료율 |
| ------- | ---- | ------ |
| Phase 1 | ⏳   | 0%     |
| Phase 2 | ⏳   | 0%     |
| Phase 3 | ⏳   | 0%     |
| Phase 4 | ⏳   | 0%     |
| Phase 5 | ⏳   | 0%     |

**전체 진행률**: 0% (0/5 phases)

---

## 🚫 현재 Blocker

- 없음 - 설계 완료, 구현 시작 가능

---

## 📝 구현 시 참고사항

### 핵심 파일 구조

```
apps/client/src/
  shared/
    policy/
      types.ts           # PolicyKey, PolicyConfig, PolicyResult
      constants.ts       # POLICY_TABLE
      hooks.ts           # useAppPolicy()
      index.ts           # exports

  entities/
    schedule/
      ui/
        ManualScheduleForm.tsx
    expense/
      ui/
        ManualExpenseForm.tsx
```

### 구현 순서 권장사항

1. **먼저 Phase 1 완료** → useAppPolicy() 작동 확인
2. **Phase 2 중 SmartMapView 우선** → 가장 영향 큰 기능
3. **Phase 3 Schedule Manual Input 우선** → Expense보다 복잡
4. **Phase 4는 점진적으로** → 한 번에 하지 말고 하나씩
5. **Phase 5는 마지막** → 모든 구현 완료 후

### 주의사항

- `useAppPolicy()`는 항상 현재 상태 기반으로 실시간 정책 반환
- Policy 변경 시 UI가 자동으로 업데이트되도록 설계
- Manual Input은 "기본 모드"가 아닌 "Fallback"으로 구현
- 네트워크 복구 시 데이터 보강은 백그라운드에서 조용히 수행

### 테스트 시나리오 예시

```typescript
// Scenario 1: 온라인 + 활성화
describe('online_active', () => {
  it('should allow all features', () => {
    const policy = useAppPolicy();
    expect(policy.createTrip.allowed).toBe(true);
    expect(policy.mapProvider).toBe('google');
    expect(policy.searchMode).toBe('api');
  });
});

// Scenario 2: 오프라인 + 활성화
describe('offline_active', () => {
  it('should block trip creation but allow schedule manual input', () => {
    const policy = useAppPolicy();
    expect(policy.createTrip.allowed).toBe(false);
    expect(policy.createSchedule.allowed).toBe(true);
    expect(policy.searchMode).toBe('manual');
  });
});
```

---

## 🔗 관련 문서

- [Policy Architecture Guide](../.claude/core/policy-architecture.md) - 구현 가이드 (520줄)
- [Manual Input Guide](../.claude/features/manual-input.md) - 기능 명세 (568줄)
- [Decision: Data/Service Separation](../.claude/decisions/2025-11-20-data-service-separation.md)
- [CHANGELOG.md](../.claude/CHANGELOG.md) - v3.0 항목

---

## 💡 Tips

- 구현 중 막히면 policy-architecture.md의 코드 예시 참고
- Phase 완료 시마다 이 문서의 체크박스 업데이트
- 각 Phase 완료 후 CHANGELOG.md에도 기록 추가
- 구현 완료 후 설계 문서의 상태 헤더를 "✅ 구현 완료"로 변경

# Noline Claude Commands

이 디렉토리는 Claude command reference를 보존한다.

- active engineering policy가 아니다.
- Codex command나 rule로 자동 이식하지 않는다.
- 예시 경로, commit hash, 정책 목록은 작성 당시 템플릿일 수 있으므로 현재 코드를 확인한다.
- 실제 작업 정책은 루트 `CLAUDE.md`, `.claude/README.md`, `rules/`, `guards/`, `runbooks/`, `context/`를 우선한다.
